{
  "name": "bas-backend-testing",
  "version": "1.0.0",
  "description": "Firebase security rules testing for BAS rituals app",
  "scripts": {
    "test": "node backend/rules-test.js",
    "emulators:start": "firebase emulators:start",
    "emulators:test": "firebase emulators:exec --only firestore,auth 'npm test'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase-admin": "^12.0.0"
  }
}